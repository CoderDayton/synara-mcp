"""Bio-inspired neural primitives used by the high-level memory ops.

These modules are pure mechanisms (CA3 pattern completion, DG sparse
separation, theta-style segmentation, successor representation) with
no dependency on the service surface beyond ``TYPE_CHECKING``.
"""
